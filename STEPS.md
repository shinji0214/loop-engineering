# Multi-Agent Dev/Review System — STEP目標

Developer Agent と Reviewer Agent を分離し、レビューまで含めた開発サイクルを
自動化するためのロードマップ。Loop Engineering の考え方に沿って、
各STEPに「完了基準（目標契約）」を設定する。

---

## STEP 1. 役割設計（目標契約を決める）

**目的**: コードを書き始める前に、エージェントの役割と合格ラインを明文化する。

**やること**
- Developer Agent の system prompt を確定する（役割・制約・出力フォーマット）
- Reviewer Agent の system prompt を確定する（チェック観点・判定フォーマット）
- Developer と Reviewer には**別モデル**を割り当てる（自己採点の甘さを防止）
- 完了基準を数値・条件で定義する（例: 「セキュリティ指摘0件」「必須テスト全通過」）
- 停止条件を定義する（最大ラウンド数 / タイムアウト / 人間へのエスカレーション条件）

**完了基準（Acceptance Criteria）**
- [ ] Developer / Reviewer 両方の system prompt が文書化されている
- [ ] 「APPROVE」判定の条件が誰が読んでも同じ解釈になる形で定義されている
- [ ] 最大ラウンド数が決まっている（推奨初期値: 5）

**成果物**: `AGENTS.md`（役割定義書）

---

## STEP 2. 最小ループの実装

**目的**: Developer ⇄ Reviewer が実際に往復するループを、UIなしでまず動かす。

**やること**
- Anthropic API を直接呼び出す最小スクリプトを作成する（`index.js`）
- 1ラウンド = Developer生成 → Reviewer判定 の往復を実装する
- APPROVE が出るか、最大ラウンドに達したら停止する
- 各ラウンドのやり取りをコンソール出力・ログファイルに残す
- `runLoop()` を `export` し、STEP 3以降でExpress等から再利用できる形にする

**完了基準**
- [ ] `node index.js "<タスク>"` で実行でき、ラウンドごとのやり取りが表示される
- [ ] APPROVEで正しく終了する
- [ ] 最大ラウンド到達時に `max_rounds_reached` として正しく終了する
- [ ] ログが `logs/` 配下にラウンドごとに保存される

**成果物**: `index.js`, `package.json`, `logs/`

---

## STEP 3. チャットツール化（UI実装）

**目的**: STEP 2 のループを、人間が見て分かる「AI同士のチャット画面」にする。

**やること**
- 簡易Web UIを実装する（Developer発言・Reviewer発言を吹き出しで色分け表示）
- バックエンドは STEP 2 のループ処理をAPI化する（例: FastAPI）
- ラウンド実行中はストリーミング or ポーリングで進捗を画面に反映する
- タスク入力フォームを用意する

**完了基準**
- [ ] ブラウザからタスクを入力して実行できる
- [ ] Developer / Reviewer の発言が視覚的に区別できる
- [ ] 実行中の状態（何ラウンド目か）が画面上で分かる
- [ ] 完了時に最終コードと判定結果がひと目で分かる

**成果物**: `app/`（バックエンド）, `web/`（フロントエンド）

---

## STEP 4. 証拠駆動化（Evidence-based Feedback）

**目的**: 「Reviewerが口頭でOKと言った」だけでなく、実行結果に基づく判定にする。

**やること**
- Developer Agent が生成したコードに対して自動テスト・lintを実行する仕組みを追加する
- テスト結果・lint結果を Reviewer Agent の入力に含める
- Reviewer Agent の判定フォーマットに「参照した証拠」を含めさせる
- テストが存在しないタスクの場合は、最低限の動作確認スクリプトを Developer Agent に生成させる

**完了基準**
- [ ] Reviewer Agent の判定にテスト実行結果が反映されている
- [ ] テスト失敗時は自動的に「差し戻し」判定になる
- [ ] 判定理由に具体的なログ・エラーメッセージが引用されている

**成果物**: `evidence/`（テスト実行結果の保存先）, Reviewer prompt の更新版

---

## STEP 5. 安全弁の実装（運用に向けて）

**目的**: 暴走・コスト超過・誤った自動承認を防ぐガードレールを整える。

**やること**
- 最大ラウンド数・タイムアウトの強制終了処理を実装する
- APIコール数・概算コストの上限を設定し、超過時に自動停止する
- 最終マージ前に人間承認を必須にするフラグを追加する
- 異常系（Reviewerが判定不能な応答を返す等）のフォールバック処理を実装する

**完了基準**
- [ ] コスト上限を超えると自動的に停止し、理由がログに残る
- [ ] 人間承認待ちの状態がUI上で明示される
- [ ] 異常系が発生してもプロセス全体がクラッシュしない

**成果物**: `safety.py`（安全弁ロジック）, 運用チェックリスト

---

## 全体の進め方

1. STEP 1 → STEP 2 まで終えたら、一度手元で実際にAIの往復を体験する
2. STEP 3 でUIを被せて「チャットツール」として体裁を整える
3. STEP 4・5 は必要に応じて後回しにしても良いが、継続運用するなら必須
4. 慣れてきたら Explorer Agent（調査役）や Test Writer Agent（テスト生成役）を追加し、
   3〜4エージェント構成に拡張する（目安として管理しやすい規模は3〜5体）

## 参考にしたLoop Engineeringの原則
- 検証可能なタスクにしか適用しない（数値化できない合否判定はループに向かない）
- 「完了」は自己申告ではなく証拠で判断する
- 停止条件を先に決める。回数を回すこと自体を目的化しない
